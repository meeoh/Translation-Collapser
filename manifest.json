{
  "name": "Github File Collapser",
  "version": "0.0.5",
  "manifest_version": 2,
  "background": {
    "scripts": ["background.js"]
  },
  "description": "Collapses files that have match supplied keywords in githubs file viewer for PR's. Defaults to files that have 'translations' in the title. List of keywords is customizable from the extension options",
  "permissions": ["tabs", "storage"],
  "content_scripts": [
    {
      "matches": ["https://github.com/*"],
      "js": ["inject.js"]
    }
  ],
  "options_ui": {
    "page": "options.html",
    "open_in_tab": false
  }
}
