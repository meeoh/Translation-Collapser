{
  "name": "Translation Collapser",
  "version": "0.0.4",
  "manifest_version": 2,
  "background": {
    "scripts": ["background.js"]
  },
  "description": "Collapses files that have 'translations' in the title in githubs file viewer for PR's",
  "permissions": ["tabs"],
  "content_scripts": [
    {
      "matches": ["https://github.com/*"],
      "js": ["inject.js"]
    }
  ]
}
